'use strict';

describe('app.contactDetail module', function () {

  beforeEach(module('app.contactDetail'));

  describe('contactDetail controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var contactDetailController = $controller('contactDetailController');
      expect(contactDetailController).toBeDefined();
    }));

  });
});