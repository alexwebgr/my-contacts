'use strict';

describe('app.contactList module', function () {

  beforeEach(module('app.contactList'));

  describe('contact-list controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var contactListController = $controller('contact-listController');
      expect(contactListController).toBeDefined();
    }));

  });
});